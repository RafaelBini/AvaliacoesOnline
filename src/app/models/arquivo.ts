export interface Arquivo {
    nomeArquivo?: string;
    caminhoArquivo?: string;
    url?: string;
    descricao?: string;
    tipo?: string,
    tipoCompleto?: string,
    tamanho?: number,
    percentual?: number;
}